#include<stdio.h>
#include<stdlib.h>

int criar_matriz(int linhas, int colunas)
{
    int peso;
    for(int i = 0; i < linhas; i++)
        for(int j = 0; j < colunas; j++)
            scanf("%d", )
}
